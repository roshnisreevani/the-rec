// MOCK DATA — Groups and Feed don't exist yet, so there's no real activity to
// count. These are hardcoded placeholders for the profile stat row and streak
// counter. Replace once real group membership / session attendance exists:
//   - groupsCount  -> count of groups the user belongs to
//   - gamesCount   -> count of sessions/games they've attended
//   - streakWeeks  -> consecutive weeks with at least one logged appearance

export const MOCK_PENNIES_COUNT = 12;
export const MOCK_GROUPS_COUNT = 3;
export const MOCK_GAMES_COUNT = 27;
export const MOCK_STREAK_WEEKS = 3;
